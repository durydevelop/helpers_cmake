## curl
message(">> Finding curl...")
set(CURL_INCLUDE_DIR "C:/curl/include")
if (CMAKE_SYSTEM_NAME STREQUAL "Windows")
    message(">> for Windows")
    add_definitions("-DCURL_STATICLIB")
endif()
#list(APPEND CMAKE_FIND_LIBRARY_SUFFIXES ".dll.a" ".a")
set(CURL_LIBRARY "C:/curl/lib")
#list(APPEND CMAKE_PREFIX_PATH "C:/curl/include")
find_package(CURL REQUIRED)
if (CURL_FOUND)
    message(">> Curl version: " ${CURL_VERSION_STRING})
    message(">> Curl include dir : " ${CURL_INCLUDE_DIRS})
    message(">> Curl libraries: " ${CURL_LIBRARIES})
    include_directories(${CURL_INCLUDE_DIRS})
    #link_directories( ${CURL_LIBRARIES} )
endif()
